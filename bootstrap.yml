apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-bootstrap
spec:
  destination:
    name: in-cluster
    namespace: argocd
  source:
    path: apps
    repoURL: https://github.com/Emrio/argocd-test
    targetRevision: HEAD
  project: default
